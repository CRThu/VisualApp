<UserControl x:Class="VisualApp.View.DataPlotView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VisualApp.View"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:vm="clr-namespace:VisualApp.ViewModel"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:sp="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
             xmlns:b="clr-namespace:VisualApp.Behaviors"
             d:DataContext="{d:DesignInstance Type=vm:DataPlotTabVM}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <hc:SimpleStackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
            <CheckBox VerticalAlignment="Center" Width="20" IsChecked="{Binding IsXDataEnabled}"/>
            <TextBlock Text="X" VerticalAlignment="Center" Width="20"/>
            <hc:ComboBox Width="300" ItemsSource="{Binding DataKeys}" SelectedItem="{Binding SelectedXDataKey}"/>
            <CheckBox Content="DateTime" IsChecked="{Binding IsXDataDateTime}" VerticalAlignment="Center" Margin="5"/>
        </hc:SimpleStackPanel>
        <hc:SimpleStackPanel Grid.Row="1" Orientation="Horizontal" Margin="5">
            <TextBlock Text="Y" VerticalAlignment="Center" Width="20" Margin="20,0,0,0"/>
            <hc:ComboBox Width="300" ItemsSource="{Binding DataKeys}" SelectedItem="{Binding SelectedYDataKey}"/>
        </hc:SimpleStackPanel>
        <hc:SimpleStackPanel Grid.Row="2" Orientation="Horizontal" Margin="5">
            <TextBlock Text="{Binding RenderStatusText}" VerticalAlignment="Center" Margin="20,0,0,0"/>
        </hc:SimpleStackPanel>
        <sp:WpfPlot Grid.Row="3">
            <i:Interaction.Behaviors>
                <b:ScottPlotBehavior Plot="{Binding Plot, Mode=OneWayToSource}" IsAutoUpdateEnabled="true"/>
            </i:Interaction.Behaviors>
        </sp:WpfPlot>
    </Grid>
</UserControl>
